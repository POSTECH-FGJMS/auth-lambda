{
  "name": "auth-lambda",
  "version": "1.0.0",
  "description": "Compress-Archive -Path .\\dist\\* -DestinationPath authenticate_user.zip -Force",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "jsonwebtoken": "^9.0.2"
  }
}
